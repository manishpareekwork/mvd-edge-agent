# Legacy Reader Scripts

These scripts are preserved from early reader bring-up and protocol discovery.
They may contain hardcoded serial ports or experimental commands.

Use the supported edge agent entrypoint for live POC operation:

```bash
PYTHONPATH=edge-agent/src python -m mvd_edge.app
```
